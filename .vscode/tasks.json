{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "0.1.0",
    "command": "${workspaceRoot}/build.py",
    "isShellCommand": true,
    "showOutput": "always",
    "args": [],
    "tasks": [
        {
            "taskName": "build",
            "isBuildCommand": true,
            // You must set the version number in the arguments field when bumping the XPI package version.
			"args": ["1.1.6"],
            "showOutput": "always"
        }
    ]
}